augment(x, ...)
boot_augment(data, times = 25, strata = NULL, apparent = FALSE, ..f, ..., augment.args = list)
boot_glance(data, times = 25, strata = NULL, apparent = FALSE, ..f, ...)
boot_tidy(data, times = 25, strata = NULL, apparent = FALSE, ..f, ..., tidy.args = list)
glance(x, ...)
grouped_augment(data, grouping.vars, ..f, ..., augment.args = list)
grouped_glance(data, grouping.vars, ..f, ...)
grouped_tidy(data, grouping.vars, ..f, ..., tidy.args = list)
tidy(x, ...)
